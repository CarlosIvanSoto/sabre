/** Used to specify the amount to be applied to the second */
export interface Fare {
    /**
     * Used to specify the amount to be applied to the second FOP.
     * @example "100.00"
     */
    Amount: string;
}